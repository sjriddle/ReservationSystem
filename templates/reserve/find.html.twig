{% extends 'base.html.twig' %}
{% block title %} Find Reservation {% endblock %}

{% block body %}
    <label>Enter your Reservation ID</label>

    <div class="form-group">
        <input type="number" name="find_id" placeholder="Reservation ID">
    </div>
    <div class="form-group">
        <button type="submit" onclick="getElemenet" class="btn btn-success">Find</button>
    </div>
{% endblock %}