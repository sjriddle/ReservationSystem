{% extends 'base.html.twig' %}
{% block title %} New Reservation {% endblock %}

{% block body %}
    <div class="form-group col-md-12">
        <div class="col-md-6">
    {{ form_start(form) }}
    {{ form_widget(form) }}
    {{ form_end(form) }}
        </div>
    </div>

    <br>
    <a href="/reserve/find">Edit existing reservation</a>
{% endblock %}
